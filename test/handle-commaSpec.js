import handleComma from '../../app/js/handle-comma.js';

describe('handle-comma.js test', function(){

	beforeEach(() => {
	});

	it('', () => {
		expect(image.getUrl()).toEqual('');
	});

	afterEach(() => {});
});
